{
  "name": "calendar-server",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Реальный бэкенд Calendar Booking API на Express + SQLite (better-sqlite3)",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "dev": "node --watch --env-file-if-exists=.env src/index.js",
    "seed": "node src/seed.js",
    "test": "node --test"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.8.1",
    "better-sqlite3-session-store": "^0.1.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "luxon": "^3.5.0",
    "nodemailer": "^6.10.1"
  }
}
